query Todos {
  todoCollection(first: 50) {
    edges {
      node {
        id
        title
        done
        starred
        priority
        note
        dueDate
      }
    }
  }
}

mutation CreateTodo($todo: TodoCreateInput!) {
  todoCreate(input: $todo) {
    todo {
      id
    }
  }
}
