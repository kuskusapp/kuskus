fragment Todo on Todo {
  id
  title
  done
  starred
  priority
  note
  dueDate
}

query Todos {
  todoCollection(first: 50) {
    edges {
      node {
        ...Todo
      }
    }
  }
}

mutation CreateTodo($todo: TodoCreateInput!) {
  todoCreate(input: $todo) {
    todo {
      id
    }
  }
}
